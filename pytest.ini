[pytest]
testpaths = tests
addopts =
    -v
    --strict-markers
    --alluredir=allure-results
    --clean-alluredir
markers =
    ai: tests that drive the app through Alumnium natural-language steps
    classic: traditional selector-based tests kept for the before/after comparison
    smoke: fast subset suitable for a PR gate
